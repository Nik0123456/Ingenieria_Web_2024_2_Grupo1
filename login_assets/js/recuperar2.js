document.getElementById("change-password-form").addEventListener("submit", function(event) {
    event.preventDefault(); // Previene el comportamiento predeterminado de envío de formulario
    window.location.href = "recuperar3.html"; // Redirige a recuperar3.html
});
